# Contributing to Dotfiles

## Core Philosophy
1. **Idempotency is King:** Scripts must be safe to run multiple times.
2. **Structure:**
   - `common/`: Shared config (Bash profile, Git config).
   - `windows/`: Windows-specific (PowerShell, Winget).
   - `macos/`: macOS-specific (Brew, Zsh).
